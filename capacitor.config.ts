import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Depannelagent.intervention',
  appName: 'DepannelAgent',
  webDir: 'dist'
};

export default config;
